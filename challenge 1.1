def factorial(n):
  if n == 0:
    return 1
  else:
    return n * factorial(n - 1)


# Example usage:
year = 5  # You can replace this with the desired year
result = factorial(year)
print(f"The factorial of {year} is {result}")
